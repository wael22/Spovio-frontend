import React from 'react';
import AdminDashboard from '@/components/admin/AdminDashboard';

/**
 * Admin Page - Super Admin control panel wrapper
 */
const Admin: React.FC = () => {
    return <AdminDashboard />;
};

export default Admin;
