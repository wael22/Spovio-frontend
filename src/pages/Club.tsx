import React from 'react';
import ClubDashboard from '@/components/club/ClubDashboard';

/**
 * Club Page - Club management panel wrapper
 */
const Club: React.FC = () => {
    return <ClubDashboard />;
};

export default Club;
