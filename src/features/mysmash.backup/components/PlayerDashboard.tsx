// PlayerDashboard - Utilise ThemeProvider de Spovio (next-themes)
import { PlayerDashboardContent } from './PlayerDashboardContent';

export const PlayerDashboard = () => {
    // Pas besoin de ThemeProvider ici, il est déjà dans main.tsx
    return <PlayerDashboardContent />;
};
